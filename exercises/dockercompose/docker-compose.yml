version: '3'

services:

  mongo:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - ./data/db:/data/db
    networks:
      - cygni-lab-2

networks:
  cygni-lab-2:
    driver: bridge
